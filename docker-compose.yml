api-gateway:
    build:
      context: .
      dockerfile: ./api-gateway/Dockerfile
    container_name: mudex-api-gateway
    ports: ["3000:3000"]
    depends_on:
      - auth-service
      - user-service
